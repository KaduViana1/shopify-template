:root {
  --bg: {{ settings.background_color}}
  ;
  --text: {{ settings.text_color}}
}

*,
*::after,
*::before {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background-color: var(--bg);
  color: var(--text)
}